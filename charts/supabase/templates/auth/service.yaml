apiVersion: v1
kind: Service
metadata:
  name: supabase-auth
  labels:
    app: supabase-auth
    component: auth
spec:
  type: ClusterIP
  ports:
    - port: 9999
      targetPort: 9999
      protocol: TCP
      name: http
  selector:
    app: supabase-auth
    component: auth
