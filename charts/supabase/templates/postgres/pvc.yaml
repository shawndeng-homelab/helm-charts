apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: supabase-db-pvc-v2
  labels:
    app: supabase
    component: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi